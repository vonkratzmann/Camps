<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="au.com.mysites.camps">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true"/>

    <application
        android:name=".util.AppContextProvider"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:installLocation="auto"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- configure the file provider -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.myfileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.SummarySiteActivity"
            android:label="@string/summary_sites_title"
            android:parentActivityName=".activities.MainActivity"/>

        <activity
            android:name=".activities.AddOrEditSiteActivity"
            android:label="@string/add_edit_site_title"
            android:parentActivityName=".activities.DetailSiteActivity"/>

        <activity
            android:name=".activities.DetailSiteActivity"
            android:label="@string/site_detail_title"
            android:parentActivityName=".activities.SummarySiteActivity"/>

        <activity
            android:name=".activities.DetailSiteMapActivity"
            android:label="@string/site_detail_map_title"
            android:parentActivityName=".activities.DetailSiteActivity"/>

        <activity
            android:name=".activities.BackUpRestoreActivity"
            android:label="@string/backup_restore_database_title"
            android:parentActivityName=".activities.SummarySiteActivity"/>

        <activity
            android:name=".activities.RatingDetailActivity"
            android:label="@string/ratings_title"
            android:parentActivityName=".activities.DetailSiteActivity"/>

        <activity
            android:name=".activities.QueryDatabaseActivity"
            android:label="@string/query_database_title"
            android:parentActivityName=".activities.SummarySiteActivity"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAfGuYRdeqfbgAI8M_GIbNMlJ6HvA8UOf0"/>

    </application>

</manifest>